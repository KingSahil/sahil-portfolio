# Project Rules

## Git Commit Message Formatting
When the user asks for a git commit message (e.g. typing "git message", "only message", "git commit message"), output ONLY the commit message text itself (title and body). Do not surround it with shell command wrappers (`git commit -m ...`), backticks, or introductory/explanatory text.
